# Base URL for the backend API (optional).
# If not set, the frontend will use the same origin.
# Example for local development with FastAPI running on 3001:
# REACT_APP_API_BASE=http://localhost:3001
REACT_APP_API_BASE=
